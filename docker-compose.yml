services:
  urbanwatch-landing:
    image: nginx:alpine
    container_name: urbanwatch-landing
    volumes:
      - ./:/usr/share/nginx/html:ro
    ports:
      - "8085:80"
    restart: unless-stopped
